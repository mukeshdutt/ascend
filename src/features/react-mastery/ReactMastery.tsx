import { useMemo, useState } from 'react'
import { Modal } from '../../shared/components/Modal'
import { Icon } from '../../shared/icons/Icon'
import { useReactMastery } from './store'
import { REACT_CATEGORIES, REACT_STATUS_LABEL, REACT_STATUSES } from './types'
import type { ReactCategory, ReactStatus, ReactTopic } from './types'
import './react-mastery.css'

type Form = Omit<ReactTopic, 'id'>
const empty = (): Form => ({ category: 'Core', title: '', status: 'not-started', resource_link: '', project_applied: '', notes: '' })
function Ring({ value, label }: { value: number; label: string }) { return <div className="mastery-ring-wrap"><div className="mastery-ring" style={{ '--value': `${value * 3.6}deg` } as React.CSSProperties}><b>{value}%</b></div><span>{label}</span></div> }
function TopicModal({ topic, onClose }: { topic: ReactTopic | null; onClose: () => void }) { const { addTopic, updateTopic, deleteTopic } = useReactMastery(); const [form, setForm] = useState<Form>(topic ? { ...topic } : empty()); const set = <K extends keyof Form>(key: K, value: Form[K]) => setForm(f => ({ ...f, [key]: value })); const save = () => { if (!form.title.trim()) return; const value = { ...form, title: form.title.trim() }; if (topic) updateTopic(topic.id, value); else addTopic(value); onClose() }; return <Modal title={topic ? 'Edit React topic' : 'Add React topic'} subtitle="Capture the theory and where you used it in practice." onClose={onClose} size="lg" footer={<>{topic && <button className="btn-danger" onClick={() => { deleteTopic(topic.id); onClose() }}><Icon name="trash" size={15} /> Delete</button>}<span style={{ flex: 1 }} /><button className="btn-ghost" onClick={onClose}>Cancel</button><button className="btn-primary" onClick={save} disabled={!form.title.trim()}><Icon name="check" size={15} /> Save topic</button></>}><div className="form-grid react-form"><label className="fld span-2"><span>Title</span><input autoFocus value={form.title} onChange={e => set('title', e.target.value)} placeholder="e.g. Compound components" /></label><label className="fld"><span>Category</span><select value={form.category} onChange={e => set('category', e.target.value as ReactCategory)}>{REACT_CATEGORIES.map(c => <option key={c}>{c}</option>)}</select></label><label className="fld"><span>Status</span><select value={form.status} onChange={e => set('status', e.target.value as ReactStatus)}>{REACT_STATUSES.map(s => <option key={s} value={s}>{REACT_STATUS_LABEL[s]}</option>)}</select></label><label className="fld span-2"><span>Resource link</span><input type="url" value={form.resource_link} onChange={e => set('resource_link', e.target.value)} placeholder="https://..." /></label><label className="fld span-2"><span>Project applied</span><input value={form.project_applied} onChange={e => set('project_applied', e.target.value)} placeholder="e.g. iterit or interview-tracks-v3" /></label><label className="fld span-2"><span>Notes</span><textarea value={form.notes} onChange={e => set('notes', e.target.value)} placeholder="What you learned or want to remember…" /></label></div></Modal> }
export function ReactMastery() { const { topics, patchTopic } = useReactMastery(); const [category, setCategory] = useState<'all' | ReactCategory>('all'); const [status, setStatus] = useState<'all' | ReactStatus>('all'); const [query, setQuery] = useState(''); const [collapsed, setCollapsed] = useState<ReactCategory[]>([]); const [modal, setModal] = useState<ReactTopic | 'new' | null>(null); const filtered = useMemo(() => topics.filter(t => (category === 'all' || t.category === category) && (status === 'all' || t.status === status) && `${t.title} ${t.notes}`.toLowerCase().includes(query.toLowerCase())), [topics, category, status, query]); const overall = topics.length ? Math.round(topics.filter(t => t.status === 'mastered').length / topics.length * 100) : 0; const toggle = (c: ReactCategory) => setCollapsed(x => x.includes(c) ? x.filter(i => i !== c) : [...x, c]); return <div className="content react-mastery"><section className="intro"><div><h1>React Mastery</h1><p>Turn React theory into applied experience, one topic at a time.</p></div><button className="btn-primary" onClick={() => setModal('new')}><Icon name="plus" size={16} /> Add topic</button></section><section className="mastery-summary"><Ring value={overall} label="Overall mastered" />{REACT_CATEGORIES.map(c => { const rows = topics.filter(t => t.category === c); const value = rows.length ? Math.round(rows.filter(t => t.status === 'mastered').length / rows.length * 100) : 0; return <Ring key={c} value={value} label={c} /> })}</section><div className="mastery-filters"><label><Icon name="search" size={16} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search title or notes" /></label><select value={category} onChange={e => setCategory(e.target.value as typeof category)}><option value="all">All categories</option>{REACT_CATEGORIES.map(c => <option key={c}>{c}</option>)}</select><select value={status} onChange={e => setStatus(e.target.value as typeof status)}><option value="all">All statuses</option>{REACT_STATUSES.map(s => <option key={s} value={s}>{REACT_STATUS_LABEL[s]}</option>)}</select></div><div className="mastery-groups">{REACT_CATEGORIES.map(c => { const rows = filtered.filter(t => t.category === c); const isCollapsed = collapsed.includes(c); return <section className="mastery-group" key={c}><button className="group-head" onClick={() => toggle(c)}><Icon name={isCollapsed ? 'chevronRight' : 'chevronDown'} size={16} /><span>{c}</span><em>{rows.length} topics</em></button>{!isCollapsed && <div className="mastery-table-wrap"><table className="mastery-table"><thead><tr><th>Topic</th><th>Status</th><th>Project applied</th><th>Notes</th><th></th></tr></thead><tbody>{rows.map(t => <tr key={t.id}><td><button onClick={() => setModal(t)} className="react-topic-name">{t.title}</button>{t.resource_link && <a href={t.resource_link} target="_blank" rel="noreferrer">Resource ↗</a>}</td><td><select className={`react-status ${t.status}`} value={t.status} onChange={e => patchTopic(t.id, { status: e.target.value as ReactStatus })}>{REACT_STATUSES.map(s => <option key={s} value={s}>{REACT_STATUS_LABEL[s]}</option>)}</select></td><td><input className="inline-input" value={t.project_applied} onChange={e => patchTopic(t.id, { project_applied: e.target.value })} placeholder="Where did you use this?" />{t.status === 'mastered' && !t.project_applied.trim() && <span className="applied-warning">Needs project proof</span>}</td><td><input className="inline-input" value={t.notes} onChange={e => patchTopic(t.id, { notes: e.target.value })} placeholder="Add notes" /></td><td><button className="row-edit" onClick={() => setModal(t)}><Icon name="edit" size={15} /></button></td></tr>)}{rows.length === 0 && <tr><td colSpan={5} className="mastery-empty">No matching topics in this category.</td></tr>}</tbody></table></div>}</section> })}</div>{modal !== null && <TopicModal topic={modal === 'new' ? null : modal} onClose={() => setModal(null)} />}</div> }
